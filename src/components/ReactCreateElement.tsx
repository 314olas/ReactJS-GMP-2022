import React from 'react'

export default function ReactCreateElement() {
    return (
        React.createElement(
            'span',
            null,
            'createElement method'
        )
    );
}
